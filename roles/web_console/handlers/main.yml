---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Bigtop Web Console
  ansible.builtin.systemd:
    name: bigtop-web-console
    state: restarted
    daemon_reload: true
  become: true
